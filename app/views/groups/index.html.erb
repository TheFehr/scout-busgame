<p id="notice"><%= notice %></p>

<h1>Gruppen</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Telefon</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.name %></td>
        <td><%= group.phone %></td>
        <td><%= link_to 'Anzeigen', group %></td>
        <td><%= link_to 'Bearbeiten', edit_group_path(group) %></td>
        <td><%= link_to 'Löschen', group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Gruppe', new_group_path %>
